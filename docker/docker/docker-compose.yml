# read about the content here
#   https://docs.docker.com/compose/compose-file/

postgres-zen:
  container_name: postgres-zen
  image: coderdojo/postgres-zen
  command: postgres
  ports: 
    - "5432"
  expose:
    - "5432"

zen: 
  container_name: zen
  image: coderdojo/zen
  command: ./localdev.js run zen
  ports: 
    # zen runs on this port
    - "8000"
  expose:
    # expose the port of zen to other services
    - "8000"
  links: 
    - postgres-zen
  volumes:
    - ../code:/user/src/app/cp-local-development
